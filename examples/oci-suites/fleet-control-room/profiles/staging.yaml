name: Staging
description: Realistic routing backends with execution network.
default: false

network:
  mode: execution

env:
  LOG_LEVEL: info
  ENABLE_TELEMETRY: "true"
  ENABLE_GEO_FENCE: "true"
  SPIKE_MULTIPLIER: "3"

services:
  control-room:
    env:
      API_PORT: 8080
