name: CI Browser
description: Headless Chromium only, tight timeouts, deterministic mocks.
default: false

network:
  mode: execution

env:
  LOG_LEVEL: warn
  BROWSERS: chromium

services:
  storefront:
    env:
      API_PORT: 3000
      FEATURE_FLAGS: ""
