apiVersion: v1
kind: Service
metadata:
  name: {{ include "babelsuite.fullname" . }}
  labels:
    {{- include "babelsuite.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "babelsuite.selectorLabels" . | nindent 4 }}
